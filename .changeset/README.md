# Changesets

Changesets describe user-facing package changes before they are released.

Run `pnpm changeset`, select the affected package, choose the SemVer bump, and
write a short summary for users. Documentation, examples, tests, and internal
refactors do not need a changeset unless they alter published behavior.
